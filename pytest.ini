[pytest]
# Run from the `temp/` directory: pytest
# `app` is importable because temp/ (the first dir without __init__.py) is on sys.path.
asyncio_mode = auto
testpaths = app/tests
python_files = test_*.py
